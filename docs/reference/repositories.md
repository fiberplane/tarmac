# Reference Repositories

Local ignored clones used during Tarmac development:

| Path                         | Source                                   | Purpose                                                                                                      |
| ---------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `references/cursor`          | `https://github.com/cursor/cursor.git`   | Public SDK package repository metadata. The current public tree is small; use package/docs for SDK behavior. |
| `references/cursor-cookbook` | `https://github.com/cursor/cookbook.git` | Cursor SDK examples, self-hosted cloud-agent worker examples, and Cursor skill examples.                     |

Reference clones are intentionally ignored by git. Promote only small,
project-specific notes into `docs/reference/`.
